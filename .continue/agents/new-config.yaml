name: Local PopOS Config
version: 1.0.0
schema: v1

models:
  - name: Qwen 2.5 7B (Arquitecto)
    provider: ollama
    model: qwen2.5:7b
    roles:
      - chat
      - edit
      - apply

tabAutocomplete:
  name: Qwen 2.5 3B
  provider: ollama
  model: qwen2.5:3b

# ESTO ES LO QUE FALTA: La capacidad de "leer"
embeddings:
  provider: ollama
  model: nomic-embed-text

# Esto activa los comandos para invocar tu código en el chat
context:
  - provider: codebase
  - provider: files
  - provider: diff
